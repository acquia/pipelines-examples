version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - nvm list
            - echo "Auto Trigger Test"
            - composer --version
            - sleep 600
            - echo "Sleep over"
