version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - nvm list
            - nvm use 18
            - node --version
            - nvm use 20
            - node --version
