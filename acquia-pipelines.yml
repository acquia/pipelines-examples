version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - sudo /etc/init.d/mysql status
            - sudo /etc/init.d/mysql start
            - echo "Unable to start database"
