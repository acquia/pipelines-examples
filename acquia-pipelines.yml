version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "Test Accessiblity:"
            - curl  http://169.254.169.254/latest/meta-data/identity-credentials/ec2/security-credentials
            - ls
