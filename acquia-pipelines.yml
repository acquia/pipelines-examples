version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            # Try to create a docroot directory which Acquia Cloud expects to serve from. If the docroot directory already existis you will get an error in your log.
            - echo "Hello, Pipelines!" > docroot/index.html
            - nvm list
            - echo "Node issue above!"
      - terminate_test:
          type: script
          script:
            - echo "Terminating... "
            - exit 0
      - verify_termination:
          type: script
          script:
            - echo "If this is displayed, termination not successful"
