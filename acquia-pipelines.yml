version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "About to start database"
            - sudo /etc/init.d/mysql start -vvv
            - echo "Unable to start database"
