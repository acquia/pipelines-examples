version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - nvm list
            - node -v
            - source /usr/local/rvm/scripts/rvm
            - gem install saas
